# Applications 功能測試指南

## 前置作業

### 1. 確認資料表已建立
```sql
-- 在 Supabase Dashboard > SQL Editor 執行
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name = 'applications';
```

應該看到 `applications` 表存在。

### 2. 插入測試資料
在 Supabase SQL Editor 執行:
```bash
supabase-migrations/02_insert_applications_test_data.sql
```

### 3. 確認環境變數
確認 `.env.local` 包含:
```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
```

### 4. 登入測試用戶
使用以下帳號登入:
- Email: `496e0094@gmail.com`
- User ID: `acf4956d-740f-4dfd-b9da-aca3b60e61d1`

---

## 測試清單

### ✅ 基本功能測試

#### 1. 應募列表頁面 (`/dashboard/applications`)

**測試步驟:**
1. 訪問 `/dashboard/applications`
2. 應該看到 7 筆測試資料

**預期結果:**
- ✅ 顯示應募卡片/列表
- ✅ 狀態統計正確顯示數量
- ✅ 沒有 Console 錯誤

**驗證點:**
```
全て (7)
ブックマーク (2)
応募済み (1)
面談・面接 (2)
内定 (1)
応募終了 (1)
```

---

#### 2. 狀態篩選

**測試步驟:**
1. 點擊不同的狀態 Tab
2. 觀察列表更新

**預期結果:**
- ✅ "ブックマーク" 顯示 2 筆
- ✅ "応募済み" 顯示 1 筆
- ✅ "面談・面接" 顯示 2 筆
- ✅ "内定" 顯示 1 筆
- ✅ "応募終了" 顯示 1 筆

---

#### 3. 搜尋功能

**測試步驟:**
1. 在搜尋框輸入 "サンプル"
2. 輸入 "React"
3. 清空搜尋

**預期結果:**
- ✅ "サンプル" → 顯示 1 筆 (株式会社サンプル)
- ✅ "React" → 顯示 1 筆 (Tag 包含 React)
- ✅ 清空 → 恢復全部顯示

---

#### 4. 視圖切換

**測試步驟:**
1. 點擊 Grid/List 切換按鈕

**預期結果:**
- ✅ Grid View: 卡片網格排列
- ✅ List View: 表格列表排列
- ✅ 切換流暢無閃爍

---

### ✅ 新增應募測試

#### 5. 手動新增應募

**測試步驟:**
1. 點擊 "新しい応募を追加"
2. 選擇 "手動で入力"
3. 填寫表單:
   - 会社名: テスト株式会社
   - 職種: テストエンジニア
   - ステータス: ブックマーク
4. 點擊 "追加"

**預期結果:**
- ✅ Toast 顯示成功訊息
- ✅ Modal 自動關閉
- ✅ 列表自動更新顯示新資料
- ✅ "ブックマーク" 數量 +1

**驗證查詢:**
```sql
SELECT company_name, position, status 
FROM applications 
WHERE company_name = 'テスト株式会社';
```

---

#### 6. Tab 切換功能

**測試步驟:**
1. 在新增 Modal 中切換不同 Tab:
   - 基本情報
   - 応募方法
   - 給与情報
   - 日程管理
2. 填寫各 Tab 的資料

**預期結果:**
- ✅ Tab 切換順暢
- ✅ 填寫的資料保留
- ✅ 必填欄位驗證正確

---

### ✅ 編輯應募測試

#### 7. 編輯應募資料

**測試步驟:**
1. 在任一應募卡片點擊 "編集"
2. 修改公司名稱
3. 點擊 "更新"

**預期結果:**
- ✅ Modal 顯示現有資料
- ✅ 可正常編輯
- ✅ 更新成功顯示 Toast
- ✅ 列表即時更新

**驗證點:**
- 檢查 `updated_at` 時間戳是否更新

---

#### 8. 應募方法編輯

**測試步驟:**
1. 編輯一筆應募
2. 切換到 "応募方法" Tab
3. 更改 "求人サイト/リファラル/直接応募"
4. 填寫對應欄位

**預期結果:**
- ✅ 切換應募方法時表單正確更新
- ✅ 必填欄位正確驗證
- ✅ 儲存後資料正確顯示

---

### ✅ 刪除應募測試

#### 9. 刪除應募

**測試步驟:**
1. 在卡片點擊刪除圖示
2. 確認刪除

**預期結果:**
- ✅ 顯示確認對話框
- ✅ 確認後刪除成功
- ✅ Toast 顯示成功訊息
- ✅ 列表即時更新
- ✅ 狀態統計數量更新

**驗證查詢:**
```sql
SELECT COUNT(*) FROM applications;
-- 應該比原本少 1
```

---

### ✅ Kanban 視圖測試

#### 10. 狀態看板頁面 (`/dashboard/statuses`)

**測試步驟:**
1. 訪問 `/dashboard/statuses`
2. 選擇 "ステータス" 視圖

**預期結果:**
- ✅ 顯示 5 個狀態欄位
- ✅ 應募卡片正確分組
- ✅ 每欄顯示正確數量

---

#### 11. 拖拉改變狀態

**測試步驟:**
1. 拖拉一張 "ブックマーク" 卡片
2. 放到 "応募済み" 欄位
3. 觀察更新

**預期結果:**
- ✅ 卡片可以拖拉
- ✅ Drop zone 有視覺回饋
- ✅ 放下後狀態更新
- ✅ Toast 顯示更新成功
- ✅ 卡片移到新欄位
- ✅ 數量統計更新

**驗證查詢:**
```sql
SELECT company_name, status 
FROM applications 
WHERE company_name = '[拖拉的公司名]';
-- 確認 status 已更新
```

---

#### 12. 快速檢視詳細

**測試步驟:**
1. 點擊任一卡片
2. 查看詳細資訊 Modal

**預期結果:**
- ✅ Modal 顯示完整資訊
- ✅ 顯示應募方法詳細
- ✅ 顯示給與資訊
- ✅ 顯示日程安排
- ✅ 可以從詳細頁點擊編輯

---

### ✅ 行事曆視圖測試

#### 13. 行事曆視圖

**測試步驟:**
1. 在 `/dashboard/statuses` 切換到 "カレンダー"
2. 觀察面試日程顯示

**預期結果:**
- ✅ 顯示行事曆
- ✅ 有 deadline 的應募顯示在對應日期
- ✅ 點擊可查看詳細

---

## 錯誤情況測試

### ❌ 網路錯誤測試

#### 14. 離線狀態

**測試步驟:**
1. 開啟 DevTools Network tab
2. 設定為 Offline
3. 嘗試新增/編輯/刪除

**預期結果:**
- ✅ 顯示錯誤 Toast
- ✅ Console 有清楚的錯誤訊息
- ✅ 不會導致頁面崩潰

---

#### 15. 未登入狀態

**測試步驟:**
1. 登出
2. 訪問 `/dashboard/applications`

**預期結果:**
- ✅ 重定向到登入頁面
- ✅ 或顯示空狀態 (無資料)

---

### ❌ 表單驗證測試

#### 16. 必填欄位驗證

**測試步驟:**
1. 開啟新增 Modal
2. 不填寫會社名
3. 嘗試提交

**預期結果:**
- ✅ 顯示錯誤訊息 "会社名は必須です"
- ✅ 表單無法提交
- ✅ 錯誤欄位標註紅色

---

#### 17. URL 格式驗證

**測試步驟:**
1. 在 "会社URL" 輸入無效 URL
2. 提交表單

**預期結果:**
- ✅ HTML5 validation 提示
- ✅ 或自訂錯誤訊息

---

## 效能測試

### ⚡ Loading 狀態

#### 18. 載入狀態顯示

**測試步驟:**
1. 開啟 DevTools Network
2. 設定 Slow 3G
3. 重新整理頁面

**預期結果:**
- ✅ 顯示 Loading indicator
- ✅ 載入完成後顯示資料
- ✅ 不會閃爍或跳動

---

#### 19. 操作 Loading 狀態

**測試步驟:**
1. 點擊新增/編輯/刪除
2. 觀察按鈕狀態

**預期結果:**
- ✅ 按鈕顯示 Loading spinner
- ✅ 按鈕 disabled 防止重複點擊
- ✅ 完成後恢復正常

---

## 資料庫驗證

### 🗄️ 資料完整性

#### 20. 資料格式檢查

**驗證查詢:**
```sql
-- 檢查 JSONB 格式
SELECT 
  id,
  company_name,
  application_method->>'type' as method_type,
  jsonb_typeof(application_method) as method_format,
  jsonb_typeof(schedule) as schedule_format,
  jsonb_typeof(offer_salary) as offer_format
FROM applications
LIMIT 5;
```

**預期結果:**
- ✅ `method_format` = "object"
- ✅ `method_type` 為有效值 (job_site/referral/direct)
- ✅ JSONB 欄位格式正確

---

#### 21. RLS 安全性測試

**測試步驟:**
```sql
-- 在 SQL Editor 切換到不同用戶
SET request.jwt.claims = '{"sub": "different-user-id"}';

-- 嘗試查詢
SELECT * FROM applications;
```

**預期結果:**
- ✅ 只能看到自己的資料
- ✅ 無法看到其他用戶的應募

---

#### 22. Trigger 測試

**測試步驟:**
1. 更新任一應募
2. 檢查 `updated_at`

**驗證查詢:**
```sql
SELECT id, company_name, created_at, updated_at 
FROM applications 
WHERE id = '[測試的 id]';
```

**預期結果:**
- ✅ `updated_at` 自動更新為當前時間
- ✅ `created_at` 保持不變

---

## Bug 報告範本

如果發現問題,請使用以下格式回報:

```markdown
### Bug 描述
[簡短描述問題]

### 重現步驟
1. 
2. 
3. 

### 預期行為
[應該發生什麼]

### 實際行為
[實際發生什麼]

### 截圖/錯誤訊息
[如果有的話]

### 環境
- Browser: 
- 用戶 ID: 
- 時間: 

### Console 錯誤
```
[貼上 Console 錯誤]
```
```

---

## 測試完成檢查表

### 基本功能
- [ ] 列表顯示正常
- [ ] 狀態篩選正確
- [ ] 搜尋功能運作
- [ ] 視圖切換正常

### CRUD 操作
- [ ] 新增應募成功
- [ ] 編輯應募成功
- [ ] 刪除應募成功
- [ ] 資料即時更新

### Kanban 功能
- [ ] 看板顯示正確
- [ ] 拖拉改變狀態
- [ ] 卡片詳細顯示

### 進階功能
- [ ] 行事曆視圖
- [ ] 表單驗證
- [ ] Loading 狀態
- [ ] 錯誤處理

### 資料庫
- [ ] 資料格式正確
- [ ] RLS 安全性
- [ ] Trigger 運作
- [ ] 索引效能

---

## 快速測試指令

```bash
# 1. 檢查資料表
npm run db:check

# 2. 重置測試資料
npm run db:seed

# 3. 查看最新資料
npm run db:view

# 4. 本地開發
npm run dev
```

---

## 支援

如有問題,請聯繫開發團隊或查閱:
- [Supabase 整合文件](./applications-supabase-integration.md)
- [API 文件](./api-documentation.md)
- [疑難排解指南](./troubleshooting.md)

